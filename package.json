{
	"name": "react-scad",
	"private": true,
	"description": "React-SCAD monorepo",
	"keywords": ["react-scad", "openscad", "monorepo"],
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/react-scad/react-scad"
	},
	"scripts": {
		"sandbox:dev": "pnpm -C sandbox run dev",
		"sandbox:start": "pnpm -C sandbox run start",
		"build": "pnpm -r run build",
		"check": "biome check --write .",
		"format": "biome format --write .",
		"format:check": "biome format .",
		"lint": "biome lint ."
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4"
	}
}
